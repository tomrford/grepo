## Project Overview

grepo is a minimal Rust binary crate used to reserve the `grepo` package name on crates.io.

## Development Environment

- Use `nix develop` for the toolchain.
- Build: `cargo build`
- Test: `cargo test`
- Format: `cargo fmt`
- Lint: `cargo clippy`
